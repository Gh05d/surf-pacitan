// Moved to src/shared/candidates.ts (2026-06-10) so the client can reuse the
// window ranking ("best window from now" banner). This shim keeps all existing
// server/test imports working.
export * from "../shared/candidates";
