// Moved to src/shared/surfable.ts (2026-06-10) so the client can reuse the
// rating logic (limiting-factor display, spot profiles). This shim keeps all
// existing server/test imports working.
export * from "../shared/surfable";
