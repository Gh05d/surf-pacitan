# Local Claude.md

## Deployment

- Git remote: `origin` (SSH alias `github-surf-pacitan` in `~/.ssh/config` for deploy key). Push with `git push origin master` (default branch is `master`, not `main`).
